using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MiddlegroundMovement : MonoBehaviour
{
    //public void UpdatePosition()
    //{
    //    Vector3 position = this.transform.position;
    //    Vector3 cameraPosition = Camera.main.transform.position;
    //    float viewWidth = 43f;
    //    float widthFromCamera = (position.x - cameraPosition.x) / viewWidth;
    //    widthFromCamera += widthFromCamera > 0 ? 0.5f : -0.5f;
    //    int widthToFix = (int)widthFromCamera;
    //    position.x -= widthToFix * viewWidth;
    //    this.transform.position = position;
    //}
}
